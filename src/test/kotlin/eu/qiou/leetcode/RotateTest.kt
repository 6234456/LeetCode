package eu.qiou.leetcode

import org.junit.Test

class RotateTest {

    @Test
    fun run() {
        println(Rotate().run(arrayOf("eat", "tea", "tan", "ate", "nat", "bat")))
        println(Rotate().run(arrayOf()))
    }
}